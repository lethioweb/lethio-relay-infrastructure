# Volatile per-address abuse control. nginx retains only binary address keys in this bounded
# shared-memory zone; the state is not written to disk. Application-wide simultaneous upstream
# work is capped separately by the relay process.
limit_req_zone $binary_remote_addr zone=lethio_off_relay_per_ip:1m rate=6r/m;
